const ModalTitle = ({ title }) => {
  return (
    <div
      style={{
        fontSize: '20px',
        fontWeight: 600,
        marginBottom: '30px',
      }}
    >
      {title}
    </div>
  );
};

export default ModalTitle;
